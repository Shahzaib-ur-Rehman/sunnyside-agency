import React from "react";

const TwoColumnLayout = ({ children }: { children: React.ReactNode }) => {
  return <section className="tow-columns-layout-lg">{children}</section>;
};

export default TwoColumnLayout;
